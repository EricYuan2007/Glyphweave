{
  "name": "example-astro-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "glyphweave": "tsx ../../packages/cli/src/index.ts",
    "build": "pnpm glyphweave build && node scripts/sync-glyphweave-public.mjs && astro build",
    "pagefind": "pagefind --site dist"
  },
  "dependencies": {
    "astro": "^5.8.1",
    "pagefind": "1.3.0"
  }
}
